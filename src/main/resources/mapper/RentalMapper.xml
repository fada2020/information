<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.info.ais.asm.mapper.RentalMapper">
	<select id="selectAll"
		resultType="jp.co.info.ais.asm.domain.Rental">

		SELECT
		a.asset_seq,
		a.asset_number,
		r.rental_seq,
		r.rental_day,
		r.rental_user_id,
		r.purpose,
		r.speciality,
		r.applicant_id,
		r.return_day,
		r.return_user_id,
		r.return_period,
		a.status_code,
		r.storage_Location,
		r.bp_Name,
		r.rental_no,
		a.kubun_code
		FROM rental r

		JOIN asset a ON
		r.asset_seq = a.asset_seq

		AND a.status_code = '002' ORDER BY
		asset_number ASC


		LIMIT #{length} OFFSET #{start}
	</select>

	<select id="selectCount" resultType="int">
		SELECT COUNT(1)
		FROM rental


	</select>

	<select id="getSelectCodeData" parameterType="String"
		resultType="jp.co.info.ais.asm.domain.CodeDetail">
		SELECT
		code_detail_id,
		code_detail_name

		FROM code_detail where item1='${codeDetailName}'

	</select>

	<select id="getSelectProduct" parameterType="String"
		resultType="jp.co.info.ais.asm.domain.Rental">
		select p.p_s_num, i.assetnumber from itasset i
		join state s
		on s.s_number=i.s_number
		join product p on p.p_s_num=i.p_s_num
		where
		i.p_s_num=#{psNum} and i.s_number='1'

	</select>

	<select id="writeProduct" parameterType="String"
		resultType="java.util.HashMap">
		select assetNumber from itasset i where
		i.assetNumber=#{realCode}

	</select>



	<select id="searchCode"
		resultType="jp.co.info.ais.asm.domain.ProductKind">
		select p_c_num,p_c_name from productkind

	</select>

	<select id="researchRental" parameterType="String"
		resultType="jp.co.info.ais.asm.domain.Rental">
		select * from Rental
		where asset_Number=#{assetNumber}
	</select>
	<select id="selectStatusCode"
		resultType="jp.co.info.ais.asm.domain.StatusCode">
		SELECT
		code_detail_id,
		code_detail_name
		FROM code_detail where code_master_id='001'
	</select>

	<select id="selectCodeDetail"
		resultType="jp.co.info.ais.asm.domain.CodeDetail">
		SELECT
		code_master_id,
		code_detail_id,
		code_detail_name,
		item1,
		item2
		FROM code_detail where code_master_id='002'
	</select>

	<select id="selectCode"
		resultType="jp.co.info.ais.asm.domain.CodeDetail">
		SELECT
		code_detail_id,
		code_detail_name
		FROM code_detail where code_master_id='003'
	</select>
<select id="selectAsset" parameterType="String"
		resultType="jp.co.info.ais.asm.domain.Asset">
		SELECT
		asset_seq,
		asset_number,
		kubun_code
		FROM asset where asset_number=#{number}
	</select>

<select id="selectAssetList" parameterType="String"
		resultType="jp.co.info.ais.asm.domain.Asset">
		SELECT
		asset_seq,
		asset_number,
		kubun_code
		FROM asset where status_code='001' and kubun_code=#{selectedItem}
	</select>


</mapper>
