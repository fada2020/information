<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.co.info.ais.asm.mapper.LoginMapper">

<select id="selectId" resultType="Integer">
    select count(emp_id)
    from emp
    where
    emp_id =#{id}
    </select>

  <select id="selectLogin" resultType="jp.co.info.ais.asm.domain.Login">
    select
    	emp_id,
		emp_name,
		organization_code,
		use_flag,
		passwd
    from emp
    where
    emp_id =#{id}
    and
    passwd = #{pass}
     </select>
 </mapper>
